<%= form_for(@book, url: '/submits') do |f| %>
	<h4>タイトル</h4>
	<%= f.text_field :title %>
	<h4>本文</h4>
	<%= f.text_area :body %>
	<%= f.submit '投稿' %>
<% end %>

<h1>投稿一覧</h1>
<% @books.each do |book| %>
	<p>タイトル</p>
	<%= book.title %>
	<%= book.body %>
<% end %>