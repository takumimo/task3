booksのindexpage
<%= render partial: 'layouts/error_form' %>
<table>
	<thead>
		<tr>
			<th colspan = "1"></th>
			<th>Title</th>
			<th>Opinion</th>
		</tr>
	</thead>
	<tbody>
		<% @books.each do |book| %>
			<tr>
				<td><%= link_to image_tag('no_image.jpg'), user_path(current_user.id) %>
				<td><%= link_to book.title, book_path(book) %></td>
				<td><%= book.body %></td>
			</tr>
		<% end %>
	</tbody>
</table>
